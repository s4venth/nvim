local M = {}

M.icons = {
}

M.lsp = {
    auto_attach = false,
    preference = nil,
}

M.misc = {
    highlight = true,
    separator = " > ",
    depth_limit = 6,
    depth_limit_indicator = "..",
    safe_output = true,
    lazy_update_context = false,
    click = false
}

return M
